apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: pinit
spec:
  selector:
    app: pinit-gateway
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
  